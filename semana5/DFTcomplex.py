## Algoritmo de DFT
import numpy as np

# Definir constantes
N = number_points

## x = np.linspace(0, np.pi, 100)
## y = 30*np.cos(x) + 60*np.sin(2*x) + 120*np.sin(3*x)

# Declare arrays
y = signal_arryas
Ycomplex = transform_arrays

# Set up plots
def Signal(y):
    # Compute DFT and returns complex Y
    # Y_n = sum_k y_k expo(-i 2*pi*pi*n/N)

def DFT(Ycomplex):
# Call functions
Signal(y)
DFT(Ycomplex)
